[phases.setup]
# Force Node and pnpm versions (Nixpacks keys)
nixPkgs = ["nodejs_22", "pnpm-9_x"]
nodejs = "22"
pnpm = "9"

[phases.install]
cmd = "pnpm install --frozen-lockfile"

[phases.build]
cmd = "pnpm run build"

[start]
cmd = "node .output/server/index.mjs"
