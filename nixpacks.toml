[phases.setup]
# Force Node version (Nixpacks keys)
nixPkgs = ["nodejs_22"]
nodejs = "22"

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[start]
cmd = "node .output/server/index.mjs"
